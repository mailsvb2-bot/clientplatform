+            dedupe_key="provider:update:42",
+        )
+        second, second_inserted = self.states.apply(
+            actor=self.owner,
+            lead_id=other.id,
+            event=SalesConversationEvent.INBOUND_RECEIVED,
+            dedupe_key="provider:update:42",
+        )
+        self.assertTrue(first_inserted)
+        self.assertTrue(second_inserted)
+        self.assertEqual(first.current, SalesConversationState.ENGAGED)
+        self.assertEqual(second.current, SalesConversationState.ENGAGED)
+
+
+    def test_other_sales_event_with_same_provider_key_does_not_block_transition(self) -> None:
+        recorded = self.sales.record_event(
+            actor=self.owner,
+            lead_id=self.lead.id,
+            event_type="provider_evidence",
+            dedupe_key="provider:update:shared",
+            payload={"kind": "evidence"},
+        )
+        self.assertTrue(recorded)
+
+        transition, inserted = self.states.apply(
+            actor=self.owner,
+            lead_id=self.lead.id,
+            event=SalesConversationEvent.INBOUND_RECEIVED,
+            dedupe_key="provider:update:shared",
+        )
+        self.assertTrue(inserted)
+        self.assertEqual(transition.current, SalesConversationState.ENGAGED)
+
+
+
+
+if __name__ == "__main__":
+    unittest.main()
