# M7-009 production rollout request

Date: 2026-09-14

Application merge SHA validated for rollout: `2c1cb636bf5cb480007160b12f4faef25867ca5c`.

Scope: deploy the already-merged M7-009 Native Automation / Approvals Cockpit to the dedicated ClientPlatform production host. This record does not change application logic, schema, credentials, infrastructure ownership, or automation policy semantics.

Pre-deploy evidence:

- protected `main` merge completed through PR #340;
- exact merged tree passed the canonical regression gate: 3613 passed, 8 skipped;
- exact merged tree passed the coverage ratchet and post-merge GitHub checks;
- live pre-deploy probe returned HTTP 200 for `/clientplatform/cockpit` and HTTP 404 for `/clientplatform/cockpit/automation.js`, confirming M7-009 was not yet present in production;
- rollout is authorized explicitly by the owner in the active ChatGPT session;
- target is the dedicated ClientPlatform production server only.

Transport note: the interactive relay is intentionally unprivileged and cannot access the root-owned production checkout or Docker socket. The repository's marker-gated production deploy recovery workflow is therefore used as the controlled SSH transport. It validates the dedicated repository origin, clean `main` worktree, exact trigger SHA, backup/readiness/rollback boundaries, and performs the canonical ClientPlatform production deploy.

Post-deploy acceptance must confirm the production cockpit remains healthy and `/clientplatform/cockpit/automation.js` is served successfully before this rollout is considered complete.
