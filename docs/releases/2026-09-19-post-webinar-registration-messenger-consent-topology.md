# Post webinar registration messenger consent topology cleanup

Code-neutral topology carrier. After the production release is confirmed, the canonical single-main workflow removes only proven-merged non-main branches and verifies that only `main` remains.
