# Post webinar registration UX topology cleanup

Code-neutral topology carrier. After the production release is confirmed, the canonical single-main workflow removes merged non-main branches and verifies that only `main` remains.
